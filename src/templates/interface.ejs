import { Entity, PrimaryGeneratedColumn, Column } from 'typeorm';
import { RelationalWithoutTenantidEntity } from './metadata/RelationalWithoutTenantidEntity';

@Entity('<%= props.tableName %>')
export default class <%= props.nameCamelCaseUpperFirst %> extends RelationalWithoutTenantidEntity {
    @PrimaryGeneratedColumn('uuid')
    id: string;
<% props.properties.strings.forEach((propertie) => { %>
    @Column()
    <%= propertie %>: string;
<%}); %><% props.properties.numbers.forEach((propertie) => { %>
    @Column()
    <%= propertie %>: number;<% }); %>
}
