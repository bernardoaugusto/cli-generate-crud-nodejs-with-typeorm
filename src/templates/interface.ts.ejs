import { typeorm } from '@seidor-cloud-produtos/lib-seidor-common';
<% if (props.tenantid) { %>import { RelationalTenantidInterface } from './metadata/RelationalTenantid';<% } else{ %>import { RelationalWithoutTenantidInterface } from './metadata/RelationalWithoutTenantid';<% } %>

export interface <%= props.nameCamelCaseUpperFirst %>Interface extends <% if (props.tenantid) { %>RelationalTenantidInterface<% } else{ %>RelationalWithoutTenantidInterface<% } %> {<% props.properties.original.strings.forEach((propertie) => { %>
    <%= propertie %>: string;<%}); %><% props.properties.original.numbers.forEach((propertie) => { %>
    <%= propertie %>: number;<% }); %>
}

export interface <%= props.nameCamelCaseUpperFirst %>RequestGetAllInterface extends typeorm.RequestGetAllInterface {<% props.properties.original.strings.forEach((propertie) => { %>
    <%= propertie %>?: string;<%}); %><% props.properties.original.numbers.forEach((propertie) => { %>
    <%= propertie %>?: number;<% }); %>
}
